# Application
APP_NAME=Job API
DEBUG=false

# PostgreSQL (asyncpg)
DATABASE_URL=postgresql+asyncpg://user:password@host:5432/dbname

# Redis (Upstash with TLS — use rediss:// scheme)
REDIS_URL=rediss://default:password@endpoint.upstash.io:6379

# Rate Limiting
RATE_LIMIT_ENABLED=true
